import 'package:flutter/material.dart';

class SearchTrackTile extends StatelessWidget {
  const SearchTrackTile({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
